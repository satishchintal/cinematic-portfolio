'use client'
import {useState} from 'react';import {motion} from 'framer-motion';import {portfolio} from '@/data/portfolio';import {SectionLabel} from './About'
export default function Experience(){const [filter,setFilter]=useState('all');const items=portfolio.journey.filter(x=>filter==='all'||x.type===filter);return <section id="experience" className="section-pad content-section"><SectionLabel n="05" t="JOURNEY / EXPERIENCE + EDUCATION"/><div className="filters">{[['all','ALL'],['work','WORK EXPERIENCE'],['education','EDUCATION']].map(([v,l])=><button key={v} onClick={()=>setFilter(v)} className={filter===v?'active':''}>{l}</button>)}</div><div className="timeline">{items.map((x,i)=><motion.article layout initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} className={`timeline-item ${i%2?'right':'left'}`} key={x.title}><div className="node"/><div className="timeline-card"><small>{x.date} · {x.type.toUpperCase()}</small><h3>{x.title}</h3><p>{x.text}</p></div></motion.article>)}</div></section>}
