'use client'
import {useState} from 'react';import {portfolio} from '@/data/portfolio';import {ArrowUpRight} from 'lucide-react';import {SectionLabel} from './About'
export default function Projects(){return <section id="projects" className="section-pad content-section"><SectionLabel n="03" t="SELECTED BUILDS"/><div className="project-grid">{portfolio.projects.map(p=><TiltCard key={p.number} p={p}/>)}</div></section>}
function TiltCard({p}:{p:(typeof portfolio.projects)[number]}){const [style,setStyle]=useState({});return <article className="project-card" onMouseMove={e=>{const r=e.currentTarget.getBoundingClientRect();setStyle({transform:`perspective(900px) rotateX(${-(e.clientY-r.top)/r.height*8+4}deg) rotateY(${(e.clientX-r.left)/r.width*10-5}deg)`})}} onMouseLeave={()=>setStyle({})} style={style}><div className="project-no">{p.number}</div><div className="project-glow"/><h3>{p.title}</h3><p>{p.description}</p><div className="chips">{p.stack.map(s=><span key={s}>{s}</span>)}</div><div className="project-link">VIEW BUILD <ArrowUpRight size={16}/></div></article>}
